# استكمال ملف الموظف — ميزة تجريبية تعمل جانبياً

تجربة مستقلة عن نظام التوظيف: المرشح يفتح رابطاً سرياً، يرفع مستنداته، فتُقرأ
آلياً وتظهر نتائجها لفريق التوظيف. **لا تكتب حرفاً واحداً في بيانات المتقدم
الرسمية** — كل ما تجمعه يعيش في جداولها الخاصة حتى تقرّر أنت نقله.

## المعادلة

```
الملف → Google Vision (قراءة) → قواعد محلية (تحقق) → نموذج لغوي (عند اللبس فقط) → المرشح يؤكد → HR يراجع الاستثناءات
```

الأغلى لا يُستدعى إلا إذا عجز ما قبله: رقم آيبان يجتاز mod-97 ورقم هوية يجتاز
checksum لا يُرسلان إلى أي نموذج. المتوقَّع أن يمرّ أغلب الآيبان والهوية بلا أي
نداء ذكاء اصطناعي، وأن يحتاجه العنوان الوطني أكثر لاختلاف تنسيقه.

## الروابط

| الرابط | لمن |
|---|---|
| `/onboarding/<token>` | المرشح — رحلة الرفع (بلا تسجيل دخول، صالح 30 يوماً) |
| `/admin/onboarding/view/:applicantId` | HR — البيانات والمرفقات وقرارات المراجعة |
| بطاقة «ملف الاستكمال» في صفحة تفاصيل المتقدم | نقطة الدخول: إنشاء الرابط ومتابعة التقدّم |

## التشغيل

الميزة تعمل الآن بلا أي مفتاح: المرشح يرفع المستند ويُدخل الحقول يدوياً،
والقواعد المحلية تتحقق منها. لتفعيل القراءة الآلية أضِف في `.env`:

```
GOOGLE_VISION_API_KEY=...        # يفعّل OCR
AI_PROVIDER=gemini               # gemini | openai | anthropic
AI_API_KEY=...                   # يفعّل الاستنتاج عند اللبس
PUBLIC_BASE_URL=https://jobs.artalsecurity.com
ONBOARDING_ENABLED=true          # false يُطفئ الميزة كاملةً بلا نشر كود
```

تبديل المزوّد سطر واحد — بقية النظام لا تعرف من خلف `extractDocumentFields`.

## العزل

* جداول خاصة: `onboarding_sessions` / `onboarding_documents` / `onboarding_fields`
  (مخططها في `database/onboarding-db.js`، وأي فشل فيه يُعطّل الميزة وحدها).
* مجلد رفع خاص: `uploads/onboarding/<applicant_id>/`.
* صفحات بتنسيقها الخاص — لا إعادة بناء لـ `public/css/tailwind.css`.
* التعديل على الكود القائم: ثلاثة أسطر تركيب في `server.js`، وبطاقة في
  `views/applicant-detail.ejs` تملأ نفسها بـ fetch وتبقى مخفية لو تعطّلت الميزة.

للتراجع الكامل: احذف الملفات الجديدة وأسطر التركيب — ولن يتغيّر شيء في النظام.

## الملفات

```
database/onboarding-db.js       المخطط المعزول
routes/onboarding.js            المسارات (العامة والإدارية)
middleware/onboardingUpload.js  استقبال الملفات
utils/visionOcr.js              Google Vision (صور + PDF)
utils/docRules.js               التطبيع والاستخراج والتحقق (checksum, mod-97, هجري→ميلادي)
utils/aiExtract.js              واجهة مجرّدة للنموذج اللغوي
utils/docPipeline.js            ترتيب المراحل لمستند واحد
views/onboarding*.ejs           الواجهات الثلاث
```

## المستندات ومتطلباتها

الهوية/الإقامة والعنوان الوطني والآيبان مطلوبة للجميع، ورخصة القيادة تُطلب ممن
سُجّل في طلبه أنه يحمل رخصة — القاعدة في `defaultRequired()` داخل
`routes/onboarding.js`، وتُخزَّن لكل مرشح وقت إنشاء الرابط.
